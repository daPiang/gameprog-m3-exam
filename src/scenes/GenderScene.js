import { SCENE_KEYS } from "../scene_constants.js";

export class GenderScene extends Phaser.Scene {
    constructor() {
        super({
            key: SCENE_KEYS.SCENES.GENDER
        })
    }

    preload() {
    }

    create() {
    }
}